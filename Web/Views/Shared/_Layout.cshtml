<!DOCTYPE html>
<html lang="fa">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>
		پنل مدیریت
	</title>

	<!-- Styles -->
	<link rel="stylesheet" href="~/assets/plugins/fontawesome-free/css/all.min.css">
	<link rel="stylesheet" href="~/assets/dist/css/adminlte.min.css">
	<link rel="stylesheet" href="~/assets/dist/css/rtl.css">
	<link rel="stylesheet" href="~/assets/css/customStyle.css">
	<link rel="stylesheet" href="~/assets/plugins/select2/css/select2.min.css">
	<link rel="stylesheet" href="~/assets/plugins/select2-bootstrap4-theme/select2-bootstrap4.min.css">
	<link rel="stylesheet" href="~/assets/dist/css/persian-datepicker.min.css">
	<link rel="stylesheet" href="~/assets/js/sweetalert.css">
	<link rel="stylesheet" href="~/css/toastr.min.css">
	<link rel="stylesheet" href="~/css/site.css">
	<script src="~/assets/plugins/jquery/jquery.min.js"></script>
	<link rel="stylesheet" href="~/css/flatpickr.min.css">


	<script>
		window.APP_CONFIG = {
			apiBaseUrl: '@ViewBag.ApiBaseUrl'
		};
	</script>

</head>

<body class="hold-transition sidebar-mini layout-fixed" dir="rtl">
	<div id="globalLoader" style="display:none">
		<div class="gl-backdrop"></div>
		<div class="gl-box">
			<div class="spinner-border" role="status" aria-hidden="true"></div>
			<div class="mt-2 small">در حال پردازش...</div>
		</div>
	</div>
	<div class="wrapper">

		<!-- Navbar -->
		<nav class="main-header navbar navbar-expand navbar-white navbar-light">

			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" data-widget="pushmenu" href="#"><i class="fas fa-bars"></i></a>
				</li>
				<li class="nav-item d-none d-sm-inline-block">
					<a href="index.php" class="nav-link">داشبورد</a>
				</li>
			</ul>

			<ul class="navbar-nav ml-auto">
				<li class="nav-item mr-3">
					<span class="nav-link">
						<i class="fas fa-user-circle"></i>
						<span id="uiUserName">
							<?= htmlspecialchars($userName ?? '') ?>
						</span>
					</span>
				</li>

				<li class="nav-item mr-3">
					<span class="nav-link">
						<span class="badge badge-info p-2" id="uiRoleTitle">
							<?= htmlspecialchars($roleTitle ?? '') ?>
						</span>

					</span>
				</li>

				<li class="nav-item">
					<a href="javascript:void(0)" id="btnLogout" class="nav-link text-danger">
						<i class="fas fa-sign-out-alt"></i> خروج
					</a>
				</li>
			</ul>

		</nav>

		<!-- Sidebar -->
		<aside class="main-sidebar sidebar-dark-primary elevation-4">

			<a href="/" class="brand-link">
				<span class="brand-text font-weight-light">مدیریت</span>
			</a>

			<div class="sidebar">
				<nav class="mt-2">
					<!-- <ul class="nav nav-pills nav-sidebar flex-column"> -->
					<ul id="sideMenu"
						class="nav nav-pills nav-sidebar flex-column"
						data-widget="treeview"
						role="menu"
						data-accordion="false">
					</ul>

					@* 	<ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu"
						data-accordion="false">
						<li class="nav-item">
							<a href="/" class="nav-link">
								<i class="nav-icon fas fa-home"></i>
								<p>داشبورد</p>
							</a>
						</li>

						<li class="nav-item">
							<a href="/Visitor" class="nav-link">
								<i class="nav-icon fas fa-users"></i>
								<p>مراجعین</p>
							</a>
						</li>

						<li class="nav-item">
							<a href="/user" class="nav-link">
								<i class="nav-icon fas fa-user-cog"></i>
								<p>مدیریت کاربران</p>
							</a>
						</li>

						<li class="nav-item">
							<a href="/Import" class="nav-link">
								<i class="nav-icon fas fa-upload"></i>
								<p>ورود اطلاعات</p>
							</a>
						</li>

						<li class="nav-item">
							<a href="/Export" class="nav-link">
								<i class="nav-icon fas fa-file-excel"></i>
								<p>خروجی اکسل مراجعین</p>
							</a>
						</li>

						<li class="nav-item">
							<a href="/LogHistory" class="nav-link">
								<i class="nav-icon fas fa-history"></i>
								<p>تاریخچه فعالیت ها</p>
							</a>
						</li>

						<li class="nav-item">
							<a href="/Personel" class="nav-link">
								<i class="nav-icon fas fa-user-clock"></i>
								<p>مدیریت پرسنل</p>
							</a>
						</li>

						<li class="nav-item">
							<a href="/Roles" class="nav-link">
								<i class="nav-icon fas fa-user-shield"></i>
								<p>مدیریت نقش ها</p>
							</a>
						</li>

						<li class="nav-item has-treeview">
							<a href="#" class="nav-link">
								<i class="nav-icon fas fa-calendar-check"></i>
								<p>
									مدیریت ویزیت
									<i class="right fas fa-angle-left"></i>
								</p>
							</a>

							<ul class="nav nav-treeview">

								<li class="nav-item">
									<a href="/" class="nav-link">
										<i class="far fa-clock nav-icon"></i>
										<p>روزها و شیفت‌های ویزیت</p>
									</a>
								</li>

								<li class="nav-item">
									<a href="/" class="nav-link">
										<i class="far fa-calendar-plus nav-icon"></i>
										<p>ثبت نوبت مراجعه</p>
									</a>
								</li>

							</ul>
						</li>


					</ul> *@
				</nav>
			</div>

		</aside>

		<!-- Content -->
		<div class="content-wrapper p-3">
			@RenderBody()
		</div>

		<footer class="main-footer text-center">
			<strong>
				©
				سیستم مدیریت مطب دکتر نسرین روشنی
			</strong>
		</footer>

	</div>


	<script src="~/js/toastr.min.js"></script>
	<script src="~/js/SweetAlert2.min.js"></script>
	<script src="~/js/toast.js"></script>
	<script src="~/js/Public/api.js"></script>
	<script src="~/assets/plugins/jquery/jquery.min.js"></script>
	<script src="~/assets/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
	<script src="~/assets/dist/js/adminlte.min.js"></script>
	<script src="~/assets/plugins/select2/js/select2.full.min.js"></script>

	<script src="~/js/flatpickr.min.js"></script>
	<script src="~/js/flatpickr.fa.js"></script>

	<script src="~/assets/dist/js/persian-date.min.js"></script>
	<script src="~/assets/dist/js/persian-datepicker.min.js"></script>
	<script src="~/assets/js/sweetalert.min.js"></script>
	<script src="~/js/public/visitors/js/jalali.js"></script>
	<script src="~/js/Public/perm.js"></script>


	<script>
		(function () {
			const AUTH_CTX_KEY = "auth_context_v1";

			async function logout() {
				try {
					// فراخوانی API لاگ‌اوت
					await apiRequest('POST', '/api/auth/logout');

				} catch (err) {
					// حتی اگر خطا خورد، فرانت باید لاگ‌اوت شود
					console.warn("Logout API failed:", err);
				} finally {
					// پاک‌سازی کامل فرانت
					sessionStorage.removeItem(AUTH_CTX_KEY);
					sessionStorage.removeItem("api_token");
					sessionStorage.removeItem("perm_map_v1");
					try {
						delete window.PERM;
					} catch (e) {
						window.PERM = null;
					}
					// ریدایرکت
					window.location.href = '/Login';
				}
			}

			document.addEventListener("DOMContentLoaded", function () {
				const btn = document.getElementById("btnLogout");
				if (!btn) return;

				btn.addEventListener("click", function (e) {
					e.preventDefault();
					logout();
				});
			});
		})();
	</script>

	<script>
		(function () {
			const CACHE_KEY = "auth_context_v1";
			const CACHE_PERM_KEY = "perm_map_v1";

			function getCached() {
				try {
					return JSON.parse(sessionStorage.getItem(CACHE_KEY) || "null");
				} catch {
					return null;
				}
			}

			function clearCached() {
				sessionStorage.removeItem(CACHE_KEY);
				sessionStorage.removeItem(CACHE_PERM_KEY);
				sessionStorage.removeItem("api_token"); // اگر توکن رو هم اینجا نگه می‌داری

				// اگر PERM رو گلوبال نگه داشتی
				try {
					delete window.PERM;
				} catch (e) {
					window.PERM = null;
				}
			}

			function normalizeUrl(item) {
				return item?.url || item?.route || item?.href || "#";
			}

			function getVal(obj, ...keys) {
				for (const k of keys) {
					if (obj && obj[k] !== undefined && obj[k] !== null) return obj[k];
				}
				return null;
			}

			function renderUser(ctx) {
				const name = ctx?.user?.displayName || ctx?.user?.username || "";
				const role = ctx?.primaryRoleTitle || "";

				const $name = $("#uiUserName");
				const $role = $("#uiRoleTitle");

				if ($name.length) $name.text(name);
				if ($role.length) $role.text(role);
			}

			// --- ساخت درخت بر اساس ParentId ---
			function buildTreeByParentId(menu) {
				// اگر بعضی آیتم‌ها children هم داشته باشند، همه را فلت می‌کنیم
				const flat = [];
				(menu || []).forEach(m => {
					flat.push(m);
					const ch = getVal(m, "children", "Children");
					if (Array.isArray(ch) && ch.length) flat.push(...ch);
				});

				// نودها را نرمال و map می‌کنیم
				const map = new Map();
				flat.forEach(raw => {
					const id = getVal(raw, "id", "Id");
					if (id == null) return;

					// کپی نود، و children خالی
					const node = map.get(id) || {
						...raw
					};
					node.__id = id;
					node.__parentId = getVal(raw, "parentId", "ParentId");
					node.__order = getVal(raw, "displayOrder", "DisplayOrder") ?? 0;
					node.__title = getVal(raw, "title", "Title") ?? "";
					node.__icon = getVal(raw, "icon", "Icon") ?? "far fa-circle";
					node.__children = node.__children || [];
					map.set(id, node);
				});

				// وصل کردن فرزندها به والد
				const roots = [];
				map.forEach(node => {
					const pid = node.__parentId;

					// ریشه: ParentId null/undefined/0/"0"
					const isRoot = pid == null || pid === 0 || pid === "0";
					if (isRoot) {
						roots.push(node);
					} else {
						const parent = map.get(pid);
						if (parent) parent.__children.push(node);
						else roots.push(node); // اگر والد پیدا نشد، بیار ریشه تا گم نشه
					}
				});

				// سورت بازگشتی
				function sortRec(list) {
					list.sort((a, b) => (a.__order ?? 0) - (b.__order ?? 0));
					list.forEach(n => sortRec(n.__children));
				}
				sortRec(roots);

				return roots;
			}

			// --- رندر درخت ---
			function renderMenuByParentId(menu) {
				const $ul = $("#sideMenu");
				if (!$ul.length) return;

				const roots = buildTreeByParentId(menu);
				$ul.empty();

				roots.forEach(item => {
					const title = item.__title;
					const icon = item.__icon;
					const children = item.__children;

					if (children && children.length) {
						const parentId = "m_" + item.__id; // ثابت و قابل تکرار
						const $li = $(`
			<li class="nav-item has-treeview">
			  <a href="#" class="nav-link">
				<i class="nav-icon ${icon}"></i>
				<p>
				  ${title}
				  <i class="right fas fa-angle-left"></i>
				</p>
			  </a>
			  <ul class="nav nav-treeview" id="${parentId}"></ul>
			</li>
		  `);

						$ul.append($li);
						const $childUl = $("#" + parentId);

						children.forEach(ch => {
							const chTitle = ch.__title;
							const chIcon = ch.__icon || "far fa-circle";
							const chUrl = normalizeUrl(ch);

							// اگر چند سطحی خواستی، همینجا می‌تونیم recurse کنیم.
							const $chLi = $(`
			  <li class="nav-item">
				<a href="${chUrl}" class="nav-link">
				  <i class="${chIcon} nav-icon"></i>
				  <p>${chTitle}</p>
				</a>
			  </li>
			`);

							$childUl.append($chLi);
						});

					} else {
						const url = normalizeUrl(item);
						const $li = $(`
			<li class="nav-item">
			  <a href="${url}" class="nav-link">
				<i class="nav-icon ${icon}"></i>
				<p>${title}</p>
			  </a>
			</li>
		  `);
						$ul.append($li);
					}
				});
			}

			// ---- init ----
			const ctx = getCached();
			if (!ctx) {
				window.location.href = "/Login";
				return;
			}

			renderUser(ctx);
			renderMenuByParentId(ctx.menu);

			// $(document).ajaxError(function (event, xhr) {
			// 	if (xhr && xhr.status === 401) {
			// 		clearCached();
			// 		window.location.href = "/Login";
			// 	}
			// });
		})();
	</script>

	<script>
		document.addEventListener("DOMContentLoaded", function () {
			if (window.Perm) Perm.apply(document);
		});
	</script>

	@await RenderSectionAsync("Scripts", required: false)

</body>



</html>
